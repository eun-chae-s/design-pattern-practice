Application of Observer Design Pattern

We're going to see how efficient the Observer Design Pattern is in the following example:
- when a celeb publishes a new post on the Instagram, their fans and their company
get notification regarding the update (using Java 8)
- when a celeb has more followers after one week (using Java 9)